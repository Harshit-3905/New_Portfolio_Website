import { Heading, VStack, Flex } from "@chakra-ui/react";

const Backend = () => {
  return (
    <VStack>
      <Heading>Backend</Heading>
      <Flex></Flex>
    </VStack>
  );
};

export default Backend;
